# PostgreSQL Settings
DB_HOST=db
DB_PORT=5432
DB_USER=adminuser
DB_PASSWORD=Password123
DB_NAME=taskdb

# Port mapping
BACKEND_PORT=5000
FRONTEND_PORT=3000

# API URL for React App
# This should be the public URL for load balancer in production.
# For local dev, it points to the backend container.
REACT_APP_API_URL=http://localhost:5000/api